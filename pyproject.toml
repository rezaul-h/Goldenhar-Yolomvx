[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "goldenhar-yolomvx"
version = "0.1.0"
description = "Experiment skeleton for Goldenhar-CFID detection and explainability"
requires-python = ">=3.10"
